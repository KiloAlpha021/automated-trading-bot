name: m1-engineering-foundation

on:
  pull_request:

permissions:
  contents: read

jobs:
  m1-engineering-foundation:
    name: m1-engineering-foundation
    runs-on: ubuntu-latest
    steps:
      - name: Trivial local success used only for trusted-gate negative proof
        run: true
